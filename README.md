# Objective Chart.JS for Python and Django

This is a small package that allows you to use a class-based approach to defining Chart.js charts in your Django views, or in other Python applications. 

The output is a JSON object, allowing it to be passed directly into templates as a variable or via Ajax.
